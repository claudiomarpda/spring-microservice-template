spring:
  config:
    import: "optional:configserver:"

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

timeline:
  client:
    post:
      user: post
      password: password